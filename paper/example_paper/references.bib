% Bibliography for Guardrail Evaluation paper (arXiv:2511.22047v1)
%
% This paper was submitted to arXiv with a pre-compiled main.bbl
% instead of a .bib file. The .bbl contains all resolved references.
%
% DO NOT run bibtex on this example — it will overwrite main.bbl
% with an empty file and the references section will disappear.
%
% To compile this example, use only:
%   pdflatex main.tex && pdflatex main.tex
%
% For YOUR papers (not this example), use the full pipeline:
%   pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex main.tex
